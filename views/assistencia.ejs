<%
function radioTrue(id){
    var rt='';
    if (alumne.radios[id] === true) {
    rt ="checked";
    }
    return rt;
    };

//DATA D'AVUI
var today = new Date();
var dd = today.getDate();
var mm = today.getMonth()+1; //January is 0!
var yyyy = today.getFullYear();

if(dd<10) {
    dd='0'+dd
} 

if(mm<10) {
    mm='0'+mm
} 

today = dd+'/'+mm+'/'+yyyy;
%>

<div class="container">
<div class="row">
            <div class="col-md-2">
              <h4>Llista assistencia</h4>
               DATA: <%=today%>
            </div>

            <div class="col-md-10">
              <hr>
            </div>
            <div class="col-md-12">

  <% if (Alumnes.length === 0) { %>
      <p><br />
      No hi ha alumnes registrats
      </p>
      <%} else { %>           
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Nom</th>
        <th>Curs</th>
        <th>Falta matí</th>
        <th>Retard matí</th>
        <th>Falta tarda</th>
        <th>Retard tarda</th>

      </tr>
    </thead>

    <tbody>
      <% var i; for (i=0; i < Alumnes.length; i++) {
      //ARRAY ASSIST
      var arraylng = Alumnes[i].assist.lenght;
      console.log(arraylng);
      Alumnes[i].assist.date = today;
      %>

      <form role="form" method="post" action="/alumne_ass?_method=put" id="alumne_ass">
      <tr>
      <td><%=Alumnes[i].nomAlumne + " " + Alumnes[i].cognomAlumne1 + " " + Alumnes[i].cognomAlumne2%></td>

      <td><%=Alumnes[i].curs%></td>
      <input type="hidden" name="alumneId" value ="<%=Alumnes[i]._id%>">
      <input type="hidden" name="assist.arraylng.date" value ="<%=today%>">

      <td><input type="radio" name="assist.<%=arraylng%>.mati" value ="falta"></td>
      <td><input type="radio" name="assist.<%=arraylng%>.mati" value = "retard"></td>
      <td><input type="radio" name="assist.<%=arraylng%>.tarda" value ="falta"></td>
      <td><input type="radio" name="assist.<%=arraylng%>.tarda" value = "retard"></td>
      
      <td>
            <!--/print_EE/<%=Alumnes[i].id%>-->
            <a href="" class="btn btn-primary btn-xs" role="button" target="_blank">Veure total</a>
      </td>
     </tr>
     </form>

     <% }; %><% } %>

    </tbody>
  </table>
          <div class="form-group col-md-3 col-md-offset-9" style="text-align:right;">
          <a href="/list_EE" class="btn btn-default" role="button">Cancel·la</a>
            <button type="submit" form="alumne_ass" class="btn btn-primary">Desa</button>
        </div>
</div>
</div>
</div>
